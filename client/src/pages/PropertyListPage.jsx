import React from 'react';
import Properties from './Properties';

// This component acts as a redundancy/alias for the Properties page
// adhering to the file structure requested by the user.
const PropertyListPage = () => {
    return <Properties />;
};

export default PropertyListPage;
